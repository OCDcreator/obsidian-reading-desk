# 自主决策日志

| 日期 | 决策点 | 选择 | 备选 | 为何这样选 | 若错怎么改 |
| --- | --- | --- | --- | --- | --- |
| 2026-09-18 | 产品视觉方向 | 日光下的研究仪器 | 复刻深色书架、杂志式阅读页 | Shape brief 要求融入 Obsidian 主题、服务长期研究任务且避免次生套路；本方向以密度和锚点关系而非装饰建立识别。 | 保持 token/组件边界，替换 `styles.css` 的语义 token 即可调整材质。 |
| 2026-09-18 | 高亮主存储 | 插件 data.json 为唯一索引，目标文件写入最小 `readingDesk` metadata | PDF 旁挂 JSON、仅 Canvas metadata | 插件数据能统一 PDF/EPUB/多目标与评论；目标 metadata 保持移动与反向删除可追溯。 | 可在迁移版加入旁挂导出层，不改变 `AnnotationStore` 接口。 |
| 2026-09-18 | EPUB 解析 | JSZip 解压 OPF，解析 title/creator/cover | 手填优先、引入完整 EPUB 阅读器 | 仅需提取、依赖小且可在本地测试；提取失败仍允许手填。 | 若 zip 兼容性不足，隔离的 extractor 可替换。 |
| 2026-09-18 | 表格评分交互 | 使用 1–10 的可键盘选择评分 | 五星、只显示十个星形 | 截图可见十个星形位置，但未观察到编辑映射；1–10 可保持截图的十格信息密度且完整可操作，属自研替代。 | `LibraryBook.rating` 是数值；可将 UI 映射改为五星而不迁移数据。 |
| 2026-09-18 | 目标侧原文回跳 | 注册 `reading-desk-highlight` 自定义 Obsidian URI，以 `file + highlightId` 打开 Reader | 仅使用 `obsidian://open?page=`、目标 DOM 查询 | 目标卡片需要精确回到持久化高亮；可见页码存在 1-based/0-based 转换风险，稳定高亮 id 没有歧义。 | 协议入口集中在 `main.ts`，可迁移为宿主公开 API 而不改目标文件格式。 |
| 2026-09-18 | 可选 AI 集成 | 仅桥接已启用的 OpenCodian：生成来源上下文笔记，调用其已存在的“加入当前笔记上下文”和“打开视图”命令 | 猜测第三方私有 API、内置模型 | 公开命令是安装实例中可验证的契约；未安装或命令缺失即禁用，避免假定 API。 | 新增有文档的公开桥接时，加入 `AiIntegrationService` 的显式 provider map。 |
| 2026-09-18 | PDF 裁剪远程模式 | OSS/COS 启用时仅上传，Canvas 写外链文本节点；本地模式才保存 vault PNG file node | 远程与本地均保存 PNG | 图床模式的用户意图是外链且不在 vault 留副本；本地模式保留可离线编辑的真实文件节点。 | 可增加明确“同时保留本地副本”设置，不改变当前两种模式。 |
| 2026-09-18 | 旧 Bookshelf 导入源 | 先读取真实 `.obsidian/plugins/bookshelf/metadata/*.json`，没有可读源时保留一次性资格 | 假定聚合 data.json、无源仍标记已导入 | 旧实现按每本 metadata 文件保存，空导入不能永久锁住用户的恢复路径。 | 将来检测到官方迁移 manifest 时，可在该入口扩展为多版本 adapter。 |
| 2026-09-19 | Canvas 章节可见性 | 选择或首次写入 Canvas 目标时，以当前 PDF 的完整 outline 同步全部章节树；书架分类保持可计数的筛选 chips，而非凭空加入聚合统计 | 仅在首条摘录时创建章节、按截图猜测额外统计卡 | 目录必须在尚无摘录时可见；`TargetService` 的 source+path 幂等键可保留同名章节路径。现有 chips 已提供分类计数、筛选和用户排序，参考未能证明需要第二个聚合面板。 | 若产品需要跨分类总览，另加明确的聚合视图而不改变筛选与持久化模型。 |
| 2026-09-19 | 目标侧评论入口 | 在 Reader 的“本页摘录卡片”管理面板为每条 Reading Desk 摘录提供“评论 +”，跳回对应页并复用同一个评论浮层和 annotation store | 向宿主 Canvas/Excalidraw 注入仿制工具条或私有事件 | 截图证明目标卡片应可发起评论，但未证明各宿主的内部扩展契约；统一管理面板可验证、不会改写普通节点，也保证三类目标共享同一评论数据。 | 宿主提供稳定公开 action API 后，可让该按钮进一步调用原生位置，同时保持本入口作为可访问回退。 |
